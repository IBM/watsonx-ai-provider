# Changelog

All notable changes to `watsonx-ai-provider` are documented in this file.

From v2.0.0 onward, this file is generated by [Changesets](https://github.com/changesets/changesets) — entries appear when a "Version Packages" PR is merged. See `package/.changeset/README.md` for the contribution flow.

## 1.x history

The 1.0.0–1.0.4 line was published using the manual-version + GitHub Release workflow and the `@ibm-cloud/watsonx-ai` SDK as a backing dependency. Those releases are available on npm but are not catalogued in this file. They support AI SDK v4.

For the v1 → v2 migration guide, see the "Migration from v1.x" section of [`package/README.md`](./README.md#migration-from-v1x).
